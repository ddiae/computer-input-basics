# PROJECT PLAN: mouse-control-basic

## 프로젝트 개요

**프로젝트명:** mouse-control-basic  
**대상:** 초등학교 1~2학년 (6~8세)  
**목표:** 마우스 및 키보드 방향키 조작 능력 습득 + 소근육 발달  
**구성:** 기본 5단계 + 심화 5단계 = 총 10개 활동  
**기술 스택:** 단일 HTML 파일 (React JSX 또는 Vanilla HTML/CSS/JS)

---

## UX 공통 규칙

### 상단 하트 시스템
- 화면 상단 중앙에 하트 10개를 항상 표시
- 각 활동 미션 완료 시 해당 번호 하트가 회색 → 빨간색으로 채워짐
- 하트 채워질 때 팡파레 애니메이션 효과 (scale up → bounce)
- 완료된 하트는 영구 유지 (새로고침해도 localStorage로 저장)

### 네비게이션
- 화면 하단 또는 좌측에 단계 선택 버튼 (1~10번)
- 버튼에는 단계 아이콘 + 이름 표시
- 완료된 단계: 초록 체크 표시
- 심화 단계(6~9): 기본 단계 3개 이상 완료 시 해금, 그 전엔 잠금(🔒) 표시
- 키보드 단계(10): 기본 단계 5개 완료 시 해금

### 피드백 원칙
- 미션 성공: 밝은 색 폭죽/별 효과 + 긍정적 텍스트 ("잘했어요!", "완벽해요!")
- 미션 실패/잘못된 동작: 부드러운 흔들림 효과 (빨간색 금지 — 아이들에게 부담)
- 모든 텍스트: 큰 글씨 (최소 18px), 굵게, 쉬운 한글
- 지시문은 짧고 명확하게: "풍선을 클릭하세요!" / "별을 끌어다 바구니에 넣으세요!"

### 시각 디자인
- 배경: 밝고 따뜻한 파스텔 계열
- 캐릭터/오브젝트: 귀엽고 둥근 형태 (SVG 또는 CSS로 직접 그리기)
- 버튼: 크고 둥글게 (min 48px 높이), 터치/클릭 여유 충분히
- 폰트: 시스템 기본 폰트 (sans-serif), 아이들이 읽기 쉽게

---

## 기본 단계 (STEP 1~5)

---

### STEP 1 — 클릭 (Click)

**학습 목표:** 마우스 왼쪽 버튼 클릭 익히기  
**테마:** 하늘에 떠 있는 풍선 터뜨리기  

**게임 방식:**
- 화면에 풍선 10개가 무작위 위치에 떠 있음
- 풍선을 클릭하면 "펑!" 효과와 함께 사라짐
- 10개 모두 클릭하면 미션 완료

**난이도 설계:**
- 풍선 크기: 60~80px (클릭하기 쉬운 크기)
- 풍선은 천천히 위아래로 흔들리는 애니메이션 (CSS keyframes)
- 클릭 정확도 요구 없음 — 풍선 영역 어디든 클릭하면 성공

**완료 조건:** 10개 풍선 전부 클릭

---

### STEP 2 — 더블클릭 (Double Click)

**학습 목표:** 마우스 더블클릭 타이밍 익히기  
**테마:** 꽃씨를 더블클릭해서 꽃 피우기  

**게임 방식:**
- 화면에 꽃씨(갈색 작은 원) 5개 배치
- 꽃씨를 더블클릭하면 꽃이 피어나는 애니메이션 재생
- 5개 모두 꽃을 피우면 미션 완료

**난이도 설계:**
- 더블클릭 인식 간격: 600ms 이내 (표준보다 넉넉하게)
- 싱글클릭만 하면 꽃씨가 살짝 흔들리며 "두 번 눌러봐요!" 힌트 표시
- 꽃 피는 애니메이션: scale 0 → 1, 색상 변화 (CSS transition)

**완료 조건:** 5개 꽃씨 모두 더블클릭으로 꽃 피우기

---

### STEP 3 — 드래그앤드롭 (Drag & Drop)

**학습 목표:** 마우스 버튼을 누른 채로 끌어다 놓기  
**테마:** 동물을 알맞은 집에 데려다 주기  

**게임 방식:**
- 화면 왼쪽: 동물 카드 4개 (강아지, 고양이, 물고기, 새)
- 화면 오른쪽: 집 영역 4개 (개집, 고양이집, 어항, 새장)
- 동물을 드래그해서 알맞은 집에 드롭
- 올바른 집에 드롭: 초록 반짝임 효과
- 틀린 집에 드롭: 부드럽게 원위치로 돌아감

**구현 방식:**
- mousedown → mousemove → mouseup 이벤트 직접 처리
- 드래그 중 커서 위치에 카드가 따라다님 (position: fixed, pointer-events: none)
- 드롭 영역: 100x100px 이상으로 충분히 크게

**완료 조건:** 4개 동물 모두 올바른 집에 드롭

---

### STEP 4 — 선 긋기 (Mouse Drawing)

**학습 목표:** 마우스를 누른 채로 이동하며 선 긋기  
**테마:** 점선을 따라 그림 완성하기  

**게임 방식:**
- Canvas 위에 점선으로 된 그림 윤곽선 표시 (별, 하트, 집 등 단순한 형태)
- 마우스를 누른 채로 점선 위를 따라 그으면 컬러 선이 그려짐
- 점선 경로의 70% 이상 덮으면 미션 완료

**구현 방식:**
- HTML Canvas API 사용
- mousedown → mousemove로 선 그리기 (ctx.lineTo)
- 점선과의 근접도 계산으로 완성도 퍼센트 표시
- 하단에 진행도 바 (Progress bar) 표시

**완료 조건:** 점선의 70% 이상 따라 그리기

---

### STEP 5 — 선 잇기 (Connect the Dots)

**학습 목표:** 순서에 맞게 점을 클릭하며 선 잇기  
**테마:** 숫자 순서대로 점을 이어 그림 완성하기  

**게임 방식:**
- 화면에 번호가 있는 점 10개 배치 (1번~10번)
- 1번부터 순서대로 클릭하면 점 사이에 선이 그려짐
- 10번까지 모두 이으면 그림 완성 (별, 물고기 등)
- 순서가 틀리면 "숫자 순서대로 눌러요!" 힌트

**구현 방식:**
- SVG 또는 Canvas로 점과 선 그리기
- 현재 클릭해야 할 번호를 크게 강조 표시
- 완성된 그림에 색칠 애니메이션

**완료 조건:** 1번~10번 순서대로 모두 클릭

---

## 심화 단계 (STEP 6~9)
*기본 단계 3개 이상 완료 시 해금*

---

### STEP 6 — 정밀 클릭 (Precision Click)

**학습 목표:** 점점 작아지는 타겟에 정확히 클릭하기  
**테마:** 움직이는 과녁 맞추기  

**게임 방식:**
- 화면에 원형 타겟이 나타남
- 타겟을 클릭할 때마다 다음 타겟은 더 작아짐 (80px → 60px → 40px → 30px → 20px)
- 10개 연속 클릭 성공하면 미션 완료
- 타겟은 클릭 후 새 위치에 랜덤하게 나타남 (화면 경계에서 충분히 떨어진 위치)

**완료 조건:** 10개 타겟 모두 클릭 성공

---

### STEP 7 — 빠른 클릭 (Speed Click)

**학습 목표:** 제한 시간 안에 빠르게 클릭하기  
**테마:** 두더지 잡기 게임  

**게임 방식:**
- 10개의 구멍이 있는 들판 화면
- 두더지가 무작위로 구멍에서 나왔다 들어감 (1.5초마다)
- 나온 두더지를 클릭하면 점수 +1
- 30초 안에 15마리 이상 클릭하면 미션 완료

**완료 조건:** 30초 내 두더지 15마리 이상 클릭

---

### STEP 8 — 정밀 드래그 (Precision Drag)

**학습 목표:** 좁은 경로를 따라 드래그하기  
**테마:** 미로 안의 공을 굴려서 출구까지 이동  

**게임 방식:**
- 단순한 미로 형태 (5x5 격자, 경로 너비 40px)
- 공을 드래그해서 미로 출구까지 이동
- 벽에 닿으면 출발점으로 돌아감
- 출구 도달 시 미션 완료

**구현 방식:**
- Canvas에 미로 그리기
- 공 위치 = 마우스 위치 (mousedown 상태일 때만)
- 충돌 감지: 공 중심이 벽 픽셀에 닿으면 리셋

**완료 조건:** 드래그로 미로 출구 도달

---

### STEP 9 — 복합 미션 (Multi-Action Challenge)

**학습 목표:** 클릭, 더블클릭, 드래그를 순서대로 수행  
**테마:** 요리사가 되어 음식 만들기  

**게임 방식:**
- 3단계 순서 미션:
  1. 재료 클릭 (클릭 x3)
  2. 냄비 더블클릭으로 불 켜기 (더블클릭)
  3. 재료를 냄비로 드래그앤드롭 (드래그 x3)
- 각 단계 완료 시 다음 단계 지시문 표시
- 모든 단계 완료 시 요리 완성 애니메이션

**완료 조건:** 3단계 순서 미션 전부 완료

---

## 키보드 단계 (STEP 10)
*기본 단계 5개 완료 시 해금*

---

### STEP 10 — 무한의 계단 (Infinite Staircase)

**학습 목표:** 키보드 방향키 4개(↑↓←→)로 캐릭터 이동 + 점프  
**테마:** 메이플스토리 스타일 2D 플랫포머  

**게임 방식:**
- 화면: 2D 사이드뷰 플랫폼 게임
- 캐릭터: 귀여운 동물 캐릭터 (CSS로 직접 그린 단순 형태)
- 조작:
  - ← → : 좌우 이동
  - ↑ 또는 위 방향키: 점프 (점프 중 재점프 불가)
  - ↓: 아래 플랫폼으로 내려가기 (통과 가능 발판)
- 맵 구성: 계단형으로 높은 곳에 배치된 플랫폼 5~7개
- 목표: 가장 높은 곳에 있는 별(★)에 도달하기
- 떨어지면 마지막 발판에서 재시작 (처음부터 X)
- 별에 도달하면 미션 완료

**물리 구현:**
- 중력: 매 프레임 velocityY += 0.5 (requestAnimationFrame)
- 점프력: velocityY = -12
- 이동 속도: velocityX = ±4
- 플랫폼 충돌: AABB (Axis-Aligned Bounding Box)

**맵 예시:**
```
                    [★]
              [====]
         [====]
    [====]
[캐릭터 시작]
```

**완료 조건:** 최상단 별(★)에 캐릭터 도달

---

## 파일 구조

```
mouse-control-basic/
├── index.html          ← 메인 파일 (모든 기능 포함)
└── README.md
```

단일 HTML 파일로 구현. 외부 라이브러리 없이 순수 HTML/CSS/JavaScript만 사용.

---

## 전체 상태 관리 (JavaScript)

```javascript
const state = {
  completedSteps: [],          // 완료된 단계 번호 배열 [1, 2, 3, ...]
  currentStep: 1,              // 현재 보고 있는 단계
  hearts: Array(10).fill(false) // 하트 채움 여부 [false, false, ...]
};

// 단계 완료 처리
function completeStep(stepNumber) {
  if (!state.completedSteps.includes(stepNumber)) {
    state.completedSteps.push(stepNumber);
    state.hearts[stepNumber - 1] = true;
    localStorage.setItem('mcb_progress', JSON.stringify(state));
    animateHeart(stepNumber);    // 하트 채우기 애니메이션
    showCelebration();           // 완료 축하 효과
    updateNavButtons();          // 잠금 해제 여부 업데이트
  }
}

// 심화 단계 잠금 해제 조건
function isUnlocked(stepNumber) {
  if (stepNumber <= 5) return true;                              // 기본: 항상 해금
  if (stepNumber <= 9) return state.completedSteps.filter(s => s <= 5).length >= 3; // 심화: 기본 3개 이상
  if (stepNumber === 10) return state.completedSteps.filter(s => s <= 5).length >= 5; // 키보드: 기본 전부
  return false;
}
```

---

## 하트 애니메이션 명세

```css
/* 하트 채워지는 애니메이션 */
@keyframes heartFill {
  0%   { transform: scale(1);   color: #ccc; }
  30%  { transform: scale(1.5); color: #ff6b6b; }
  60%  { transform: scale(0.9); color: #ff4444; }
  100% { transform: scale(1);   color: #ff4444; }
}

.heart.filled {
  animation: heartFill 0.6s ease forwards;
}
```

---

## 단계별 지시문 (아이용 한글)

| 단계 | 화면에 표시되는 지시문 |
|------|----------------------|
| 1 | "풍선을 모두 클릭해서 터뜨려요! 🎈" |
| 2 | "꽃씨를 두 번 빠르게 눌러서 꽃을 피워요! 🌸" |
| 3 | "동물을 끌어다 알맞은 집에 놓아줘요! 🏠" |
| 4 | "점선을 따라 마우스를 움직여 그림을 완성해요! ✏️" |
| 5 | "숫자 순서대로 점을 클릭해서 이어요! 🔢" |
| 6 | "움직이는 동그라미를 클릭해요! 점점 작아져요! 🎯" |
| 7 | "두더지가 나오면 빨리 클릭해요! ⏰" |
| 8 | "공을 잡고 미로 끝까지 이동해요! 벽에 닿으면 안돼요! 🎱" |
| 9 | "순서대로 따라해요! 재료 클릭 → 냄비 더블클릭 → 재료 드래그! 🍳" |
| 10 | "방향키로 캐릭터를 움직여요! ↑키로 점프! 별에 도달하세요! ⭐" |

---

## 구현 우선순위

1. **먼저 구현:** 공통 레이아웃 (하트 바, 네비게이션, 상태 관리)
2. **기본 단계 순서대로:** STEP 1 → 2 → 3 → 4 → 5
3. **심화 단계:** STEP 6 → 7 → 8 → 9
4. **마지막:** STEP 10 (플랫포머 물리 엔진이 가장 복잡)

---

*이 플랜을 기반으로 단일 HTML 파일로 구현하세요. 외부 라이브러리 없이 순수 HTML/CSS/JS만 사용합니다.*
